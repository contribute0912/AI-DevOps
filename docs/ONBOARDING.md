# Onboarding Guide

Welcome to the AI-Powered DevOps Demo project! This guide will help you get started as a new contributor.

## 🚀 Quick Start
1. **Clone the repository**
   ```bash
   git clone https://github.com/YOUR_USERNAME/ai-devops-demo.git
   cd ai-devops-demo
   ```
2. **Install Node.js (>=16) and npm**
3. **Run the setup script**
   ```bash
   bash scripts/setup.sh
   ```
4. **Open the project in VS Code**
   - Install all recommended extensions when prompted
5. **Start developing!**
   ```bash
   npm start
   ```

## 🛠️ Dev Environment
- Use VS Code tasks for running, testing, linting, and formatting
- Use Copilot and snippets for code and workflow generation
- All configuration files are in the project root or `.vscode/`

## 📋 Branching & PRs
- Create a feature branch for your work
- Push and open a PR using the provided template
- Ensure all checks pass before requesting review

## 🧪 Testing & Quality
- Run `npm test` and `npm run lint` before every PR
- Maintain 80%+ test coverage
- Use `node scripts/security-check.js` for security validation

## 🔒 Security
- Never commit secrets or sensitive data
- Use `.env.example` to document required environment variables

## 🤖 AI Prompts
- See `docs/AI_PROMPTS.md` for Copilot prompt ideas
- Use Copilot Chat for DevOps and code questions

## 📚 Documentation
- Update `README.md` and `CONTRIBUTING.md` as needed
- Add new docs to the `docs/` folder

## 🆘 Troubleshooting
- See `docs/TROUBLESHOOTING.md` for common issues
- Ask questions in PRs or issues

---

**Welcome aboard! Happy coding!**
